-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.app_settings (
  key character varying NOT NULL,
  value text NOT NULL,
  category character varying NOT NULL,
  label character varying,
  is_secret boolean DEFAULT false,
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT app_settings_pkey PRIMARY KEY (key)
);
CREATE TABLE public.clients (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  name character varying NOT NULL,
  email character varying UNIQUE,
  whatsapp character varying UNIQUE,
  cpf_cnpj character varying UNIQUE,
  address text,
  notes text,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT clients_pkey PRIMARY KEY (id)
);
CREATE TABLE public.documents (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  process_id uuid NOT NULL,
  name character varying NOT NULL,
  type character varying,
  size integer,
  url text NOT NULL,
  uploaded_at timestamp without time zone DEFAULT now(),
  CONSTRAINT documents_pkey PRIMARY KEY (id),
  CONSTRAINT documents_process_id_fkey FOREIGN KEY (process_id) REFERENCES public.processes(id)
);
CREATE TABLE public.financial_transactions (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  description character varying NOT NULL,
  amount numeric NOT NULL,
  type character varying NOT NULL,
  status character varying NOT NULL DEFAULT 'pending'::character varying,
  date timestamp without time zone NOT NULL,
  client_id uuid,
  process_id uuid,
  installment_current integer,
  installment_total integer,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT financial_transactions_pkey PRIMARY KEY (id),
  CONSTRAINT financial_transactions_client_id_fkey FOREIGN KEY (client_id) REFERENCES public.clients(id),
  CONSTRAINT financial_transactions_process_id_fkey FOREIGN KEY (process_id) REFERENCES public.processes(id)
);
CREATE TABLE public.notification_logs (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  type character varying NOT NULL,
  recipient character varying NOT NULL,
  message text NOT NULL,
  status character varying NOT NULL,
  timestamp timestamp without time zone DEFAULT now(),
  CONSTRAINT notification_logs_pkey PRIMARY KEY (id)
);
CREATE TABLE public.process_clients (
  process_id uuid NOT NULL,
  client_id uuid NOT NULL,
  CONSTRAINT process_clients_pkey PRIMARY KEY (process_id, client_id),
  CONSTRAINT process_clients_process_id_fkey FOREIGN KEY (process_id) REFERENCES public.processes(id),
  CONSTRAINT process_clients_client_id_fkey FOREIGN KEY (client_id) REFERENCES public.clients(id)
);
CREATE TABLE public.process_lawyers (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  process_id uuid NOT NULL,
  lawyer_id uuid NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT process_lawyers_pkey PRIMARY KEY (id),
  CONSTRAINT process_lawyers_process_id_fkey FOREIGN KEY (process_id) REFERENCES public.processes(id),
  CONSTRAINT process_lawyers_lawyer_id_fkey FOREIGN KEY (lawyer_id) REFERENCES public.users(id)
);
CREATE TABLE public.process_status_history (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  process_id uuid NOT NULL,
  user_id uuid,
  previous_status character varying NOT NULL,
  new_status character varying NOT NULL,
  changed_at timestamp without time zone DEFAULT now(),
  CONSTRAINT process_status_history_pkey PRIMARY KEY (id),
  CONSTRAINT process_status_history_process_id_fkey FOREIGN KEY (process_id) REFERENCES public.processes(id),
  CONSTRAINT process_status_history_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.processes (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  number character varying NOT NULL UNIQUE,
  data_source character varying NOT NULL,
  status character varying NOT NULL DEFAULT 'active'::character varying CHECK (status::text = ANY (ARRAY['active'::character varying, 'archived'::character varying, 'suspended'::character varying]::text[])),
  outcome character varying CHECK (outcome::text = ANY (ARRAY['won'::character varying, 'lost'::character varying, 'settled'::character varying]::text[])),
  process_value numeric DEFAULT 0.00,
  payment_status character varying NOT NULL DEFAULT 'pending'::character varying CHECK (payment_status::text = ANY (ARRAY['pending'::character varying, 'paid'::character varying, 'overdue'::character varying]::text[])),
  description text,
  movements jsonb,
  subjects jsonb,
  parties jsonb,
  ai_analysis jsonb,
  deep_strategy jsonb,
  documents jsonb,
  external_data jsonb,
  last_update timestamp without time zone DEFAULT now(),
  created_at timestamp without time zone DEFAULT now(),
  closed_at timestamp without time zone,
  datajud_info jsonb,
  cliente_preso boolean DEFAULT false,
  manual_process_type character varying,
  datajud_not_available boolean DEFAULT false,
  CONSTRAINT processes_pkey PRIMARY KEY (id)
);
CREATE TABLE public.scheduled_reports (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  title character varying NOT NULL,
  type character varying NOT NULL,
  frequency character varying NOT NULL,
  format character varying NOT NULL,
  recipients text NOT NULL,
  active boolean DEFAULT true,
  last_run timestamp without time zone,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT scheduled_reports_pkey PRIMARY KEY (id)
);
CREATE TABLE public.tasks (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  process_id uuid NOT NULL,
  title character varying NOT NULL,
  description text,
  due_date timestamp without time zone NOT NULL,
  status character varying DEFAULT 'pending'::character varying,
  priority character varying DEFAULT 'medium'::character varying,
  notify_client boolean DEFAULT false,
  notification_sent boolean DEFAULT false,
  notification_date timestamp without time zone,
  notification_channel character varying,
  synced_with_google boolean DEFAULT false,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT tasks_pkey PRIMARY KEY (id),
  CONSTRAINT tasks_process_id_fkey FOREIGN KEY (process_id) REFERENCES public.processes(id)
);
CREATE TABLE public.timesheets (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  client_id uuid,
  process_id uuid,
  description text NOT NULL,
  start_time timestamp without time zone,
  end_time timestamp without time zone,
  duration_minutes integer NOT NULL DEFAULT 0,
  billable boolean DEFAULT true,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT timesheets_pkey PRIMARY KEY (id),
  CONSTRAINT timesheets_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT timesheets_client_id_fkey FOREIGN KEY (client_id) REFERENCES public.clients(id),
  CONSTRAINT timesheets_process_id_fkey FOREIGN KEY (process_id) REFERENCES public.processes(id)
);
CREATE TABLE public.users (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  name character varying NOT NULL,
  email character varying NOT NULL UNIQUE,
  role character varying DEFAULT 'operador'::character varying CHECK (role::text = ANY (ARRAY['admin'::character varying, 'advogado'::character varying, 'assistente'::character varying, 'operador'::character varying, 'cliente'::character varying]::text[])),
  avatar text,
  permissions jsonb,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT users_pkey PRIMARY KEY (id),
  CONSTRAINT users_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id)
);